# Archived Go backend (not used by the demo stack).

This directory contains the original incomplete Go implementation.
The active backend is `../backend` (FastAPI + SQLite).

Kept for reference only: domain models, agent prompts, and API shape.
